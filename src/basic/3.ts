let stringOrNumber: string | number;

stringOrNumber = 5;
stringOrNumber = 'Poo';
stringOrNumber = true;

type EnableOrDisable = 'enable' | 'disable';

const enable: EnableOrDisable = 'enable';
const disable: EnableOrDisable = 'disable';
const something: EnableOrDisable = 'something';
